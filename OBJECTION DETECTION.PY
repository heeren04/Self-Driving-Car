OBJECT DETECTION USING CANNY FRAMES
import cv2
import numpy as np
​
# Open a video file or capture from a camera
# If using a video file, replace 'your_video.mp4' with the path to your video file
# If using a camera, replace 0 with the camera index (usually 0 for the default camera)
cap = cv2.VideoCapture('C:/Users/lenovo/OneDrive/Desktop/test1.mp4')
​
while cap.isOpened():
    # Read a frame from the video
    ret, frame = cap.read()
​
    if not ret:
        break
​
    # Convert the frame to grayscale
    gray = cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY)
​
    # Apply Gaussian blur to the frame
    blurred = cv2.GaussianBlur(gray, (5, 5), 0)
​
    # Apply Canny edge detector
    edges = cv2.Canny(blurred, 50, 150)
​
    # Display the original frame and the frame with Canny edges
    cv2.imshow('Original Frame', frame)
    cv2.imshow('Canny Edges', edges)
​
    # Break the loop if 'q' key is pressed
    if cv2.waitKey(1) & 0xFF == ord('q'):
        break
​
# Release the video capture object and close all windows
cap.release()
cv2.destroyAllWindows()
​
